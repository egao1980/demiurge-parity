# Corporate parity stack. Default corporate profile = Postgres (+ MinIO + collector).
# LDAP / Keycloak are optional, matching demiurge/ops/docker-compose.corporate.yml.
#
#   docker compose --profile corporate up --wait
#   DEMIURGE_PARITY_TIER=live-corporate \
#     DEMIURGE_CORPORATE__POSTGRES__DSN=postgres://demiurge:demiurge@127.0.0.1:5432/demiurge \
#     ros -e '(asdf:test-system "demiurge-parity")' -q
#
# Optional: add --profile ldap and/or --profile oidc. Do not require them for S9.
services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: demiurge
      POSTGRES_PASSWORD: demiurge
      POSTGRES_DB: demiurge
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U demiurge -d demiurge"]
      interval: 5s
      timeout: 5s
      retries: 10
    profiles: ["corporate", "oidc"]

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: minio12345
    ports:
      - "9000:9000"
      - "9001:9001"
    profiles: ["corporate"]

  collector:
    image: otel/opentelemetry-collector-contrib:0.128.0
    command: ["--config=/etc/otelcol/config.yaml"]
    volumes:
      - ./otel-collector.yaml:/etc/otelcol/config.yaml:ro
    ports:
      - "4317:4317"
      - "4318:4318"
    profiles: ["corporate"]

  ldap:
    profiles: ["ldap"]
    image: osixia/openldap:1.5.0
    environment:
      LDAP_ORGANISATION: Demiurge
      LDAP_DOMAIN: example.com
      LDAP_ADMIN_PASSWORD: admin
    ports:
      - "389:389"
      - "636:636"

  keycloak:
    profiles: ["oidc"]
    image: quay.io/keycloak/keycloak:25.0
    command: ["start-dev"]
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "8081:8080"
    depends_on:
      postgres:
        condition: service_healthy

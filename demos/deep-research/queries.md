# Deep research — workspace sources (board + RAG + MCP) + per-step instructions.
# Mock-tier canned subquestions (fallback only): What is KSAR? / blackboard / journal.
# Expected: verdict, child answers, citations, synthesis report.
# Live tier uses SearXNG (docker compose --profile search) + LM Studio or llama.cpp.
CL expert systems

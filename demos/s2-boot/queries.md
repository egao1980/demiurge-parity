# S2 boot — personal profile factory in a clean temp dir.
# Expected: :PERSONAL kind; journal / session / chunker / rag / llm stores bound.
# SQLite files (journal.sqlite / sessions.sqlite / rag.sqlite) may be absent
# when the backend is in-memory.
#
# This file has no query lines. `command = "boot"` in demo.toml is enough.
